#!/usr/bin/with-contenv bash

if [ ! -d /config/doc ]; then
    echo "C++ documentation is missing, copying..."

    cp -r /defaults/doc/reference/* /config/doc
    chown -R $PUID:$PGID /config/doc
fi